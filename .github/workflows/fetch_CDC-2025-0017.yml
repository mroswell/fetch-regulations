name: Fetch Comments — CDC-2025-0017 (ACIP Apr 2025)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-comments:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pandas tqdm

      - name: Run script
        env:
          REGULATIONS_API_KEY: ${{ secrets.REGULATIONS_API_KEY }}
          DOCKET_ID: CDC-2025-0017
          MAX_MINUTES: 330
        run: python scripts/fetch_regulations_comments.py

      - name: Summary
        run: |
          echo "=== Fetch Summary ==="
          ROWS=$(tail -n +2 data/csv/comments_CDC-2025-0017.csv | wc -l | tr -d ' ')
          LAST=$(tail -1 data/csv/comments_CDC-2025-0017.csv | cut -d',' -f1)
          echo "Total rows: $ROWS"
          echo "Last comment ID: $LAST"

      - name: Upload CSV as artifact
        uses: actions/upload-artifact@v4
        with:
          name: comments-CDC-2025-0017
          path: data/csv/comments_CDC-2025-0017.csv

      - name: Commit CSV to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/csv/comments_CDC-2025-0017.csv
          git stash
          git pull --rebase
          git stash pop
          git diff --staged --quiet || git commit -m "Fetch comments for CDC-2025-0017 [skip ci]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
